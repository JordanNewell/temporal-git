{
  "name": "@temporal-git/core",
  "version": "2.1.0",
  "description": "Core git bisect engine for temporal-git (shared by CLI and VS Code extension).",
  "author": "Jordan Newell",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/JordanNewell/temporal-git",
    "directory": "packages/core"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p .",
    "dev": "tsc -p . --watch",
    "pretest": "npm run build",
    "test": "node --test \"dist/test/**/*.test.js\""
  },
  "dependencies": {
    "simple-git": "^3.19.1"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "typescript": "^5.1.6"
  }
}
